# .env — place this at the root of your ml-ids-analyzer project

# Secret API key used by FastAPI (passed into the container as ${example_key})
# Replace the placeholder value below with whatever key you want to require 
# when calling the /predict endpoints.
example_key=dev_key_12345